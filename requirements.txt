flask>=2.3.0
requests>=2.31.0
python-dotenv>=1.0.0
# gunicorn is the production WSGI server Render uses instead of Flask's
# built-in dev server (which is unsafe and not multi-worker).
gunicorn>=21.2.0
